#!/bin/bash
# Reset seeded demo state to a clean baseline. Arrives with Phase 3+
# workflows; kept as an explicit no-op so demo tooling paths are stable now.
set -euo pipefail
echo "reset_demo_state: nothing to reset in Phase 0 (workflow fixtures land in Phase 3+)"
